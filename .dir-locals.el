((nil . ((eval . (progn
                   (let ((my-project-file (expand-file-name "goto-transcoder-mode.el" (locate-dominating-file default-directory ".dir-locals.el"))))
                     (when (file-exists-p my-project-file)
                       (load my-project-file)
                       (goto-transcoder-mode 1))))))))
